# securities-futures-chaos 技能安装说明

基于《证券混沌操作法》的趋势扫描 skill（A股换手率前50 + 期货资金流入/流出前5，
道氏结构 + 分形买卖点 + 止损前低/前高，支持盘中 `--intraday` 瞬时破位判定）。

## 要求（目标电脑）
- Codex 桌面版（CLI 也可用，技能目录相同）
- Python 3.8+（脚本只用标准库，无需 pip 安装）
- 能联网访问新浪/腾讯行情接口
- （可选）若要用“WH6 加权资金榜联动”，需那台电脑装有文华 WH6 且为 Windows
  （用 Windows 自带 OCR 读榜单）；不用 WH6 时该功能自动忽略，主流程不受影响

## 安装步骤
1. 把本压缩包里的 `securities-futures-chaos` 整个文件夹，放到目标电脑的
   技能目录下（若没有就新建）：
   - Windows: `C:\Users\<你的用户名>\.codex\skills\`
   - macOS / Linux: `~/.codex/skills/`
   最终路径应形如：
   `C:\Users\<你的用户名>\.codex\skills\securities-futures-chaos\SKILL.md`
2. 完全退出并重启 Codex（新技能在重启后的下一轮对话可用）。
3. 验证：对话里问“跑一遍证券混沌扫描”或输入：
   `python <技能路径>/scripts/scan_dow_signal.py --out console`

## 常用用法（B 方案默认：期货用“连续”，全自动、无需 WH6）
```
python <技能路径>/scripts/scan_dow_signal.py --out console            # 收盘后跑最近完整交易日
python <技能路径>/scripts/scan_dow_signal.py --intraday               # 盘中随时看趋势/破位
python <技能路径>/scripts/scan_dow_signal.py --no-stocks              # 只看期货
python <技能路径>/scripts/scan_dow_signal.py --intraday --no-futures  # 盘中只看 A股
```
输出：A股做多候选(换手率前50)、期货做多(资金流入+上涨结构)、期货做空(资金流出+下跌结构)，
每条含：道氏结构、当前信号、突破/跌破位与日期、止损位(前低/前高)、强度分。

## （可选）WH6 加权资金榜联动
把 WH6 切到“品种加权排名”页并保持在前台，然后：
```
powershell -ExecutionPolicy Bypass -File <技能路径>/scripts/wh6_snapshot.ps1 -Png wh6_rank.png -Words wh6_words.txt
python <技能路径>/scripts/wh6_ocr_pool.py wh6_words.txt wh6_pool.txt
python <技能路径>/scripts/wh6_pool_scan.py --pool-file wh6_pool.txt --out markdown
```
详见 SKILL.md 末尾说明。WH6 无程序化导出接口，方案为“截图 + Windows OCR 读榜（只读）+ 新浪K线分析”。

## 提醒
- 输出是研究参考，不是投资建议；下单前请人工复核。
- 数据源为免费公开接口，若目标电脑网络受限请先 `python <技能路径>/scripts/inspect_data.py` 自检。
